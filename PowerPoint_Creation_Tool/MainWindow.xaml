<Window x:Class="PowerPointWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PowerPointWPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height=".6*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height=".6*"/>
            <RowDefinition Height="6*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Title:" FontWeight="Bold"/>

        <RichTextBox Name="TitleBox" Grid.Row="1" Margin="6,6,6,6">
            <FlowDocument>
                <Paragraph>
                    <Bold>Fast cars</Bold>
                </Paragraph>
            </FlowDocument>
        </RichTextBox>

        <Label Grid.Row="2" Content="Text:" FontWeight="Bold"/>

        <RichTextBox Name="TextBox" Grid.Row="3" Margin="6,6,6,6">
            <FlowDocument>
                <Paragraph>
                    Fast red
                    <Bold>car</Bold>
                </Paragraph>
            </FlowDocument>
        </RichTextBox>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="right">

            <Button Name="Photos" Grid.Row="5"
                Width="150" Margin="6,6,6,6"
                Click="PhotosClick">
                Photos Search
            </Button>

            <Button Name="Submit" Grid.Row="5"
                Width="150" Margin="6,6,6,6"
                Click="SubmitClick">
                Create Slide
            </Button>

            <Button Name="Exit" Grid.Row="5"
              Width="150" Margin="6,6,6,6"
              Click="ExitClick">
                Exit
            </Button>

        </StackPanel>



    </Grid>
</Window>
